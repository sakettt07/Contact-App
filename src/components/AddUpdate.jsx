import React from 'react'
import New from "./New";


const AddUpdate = ({isOpen,onClose}) => {
  return (
    <div>
      <New
    isOpen={open}
    onClose={onClose}
    >Hi</New>
    </div>
  )
}

export default AddUpdate
